-- ============================================================
-- 015_log_reactions_drop_employee.sql
-- 응원에서 "누가 눌렀는지"를 완전히 제거.
--
-- 로그인이 없는 앱이라 신뢰할 수 있는 정보가 아니었고, 화면 어디에도
-- 쓰지 않게 되어 컬럼째 정리합니다.
-- 응원 개수(행)는 그대로 남고, 누가 눌렀는지 정보만 사라집니다.
-- ============================================================

ALTER TABLE log_reactions DROP COLUMN IF EXISTS employee_id;
